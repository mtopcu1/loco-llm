name: publish

# Fallback manual publish. Normal releases publish from release-please.yml
# when releases_created is true (GITHUB_TOKEN release events do not trigger
# this workflow).
on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. v0.3.1)"
        required: true

permissions:
  contents: write   # to upload release assets
  id-token: write   # for PyPI OIDC trusted publishing

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tooling
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build wheel and sdist
        run: python -m build

      - name: Publish to PyPI via OIDC
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

      - name: Build scaffold tarball
        env:
          TAG: ${{ github.event.release.tag_name || inputs.tag }}
        run: |
          tar czf "scaffold-${TAG}.tar.gz" \
            runtimes configs benchmarks requirements.yaml
          sha256sum "scaffold-${TAG}.tar.gz" > "scaffold-${TAG}.tar.gz.sha256"

      - name: Attach assets to GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.event.release.tag_name || inputs.tag }}
        run: |
          gh release upload "${TAG}" \
            dist/*.whl \
            dist/*.tar.gz \
            "scaffold-${TAG}.tar.gz" \
            "scaffold-${TAG}.tar.gz.sha256" \
            --clobber
